# Flask secret key used for sessions and CSRF protection.
# In production, replace this with a long random value.
SECRET_KEY=change-this-secret-key

# Optional database URL.
# Leave unset to use the default local SQLite database in instance/studyhub.db.
DATABASE_URL=sqlite:///instance/studyhub.db